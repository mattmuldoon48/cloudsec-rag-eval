# Raw documentation source files

Place source markdown files in this folder. The files in this starter project are synthetic sample docs intended for demonstration only, not official AWS or NIST guidance.

After adding or updating docs, run:

```bash
python scripts/ingest_docs.py
```
